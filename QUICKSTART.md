# ⚡ Быстрый старт

## Для локального запуска:

1. **Установите зависимости:**
   ```bash
   pip install -r requirements.txt
   ```

2. **Создайте файл `.env`:**
   - Запустите `setup_local.bat` (Windows)
   - Или создайте файл `.env` вручную и добавьте:
     ```
     BOT_TOKEN=8534429029:AAFhc4gNNTco5hu3jB9xa3zfSm_hUWtevR4
     ```

3. **Запустите бота:**
   ```bash
   python bot.py
   ```

## Для деплоя на сервер (чтобы бот работал 24/7):

### 🚀 Railway (Рекомендуется - 5 минут!)

1. Зайдите на https://railway.app/
2. Зарегистрируйтесь через GitHub
3. Создайте новый проект → "Deploy from GitHub repo"
4. Добавьте переменную окружения:
   - Name: `BOT_TOKEN`
   - Value: `8534429029:AAFhc4gNNTco5hu3jB9xa3zfSm_hUWtevR4`
5. Готово! Бот работает 24/7 🎉

**Подробная инструкция:** См. файл `DEPLOY.md`

---

## 📋 Команды бота:

- `/start` - Начать интервью
- `/export_all` - Скачать Excel со всеми интервью
- `/stats` - Статистика
- `/cancel` - Отменить интервью

---

## ❓ Проблемы?

**Бот не запускается локально:**
- Проверьте, что файл `.env` существует
- Убедитесь, что токен правильный
- Проверьте, что все зависимости установлены

**Бот не отвечает после деплоя:**
- Проверьте логи на платформе (Railway/Render)
- Убедитесь, что переменная `BOT_TOKEN` установлена
- Проверьте, что бот запущен (смотрите логи)

---

**Готово!** Теперь ваш бот может работать 24/7 на сервере! 🚀

