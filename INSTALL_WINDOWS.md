# 🪟 Установка на Windows

## Проблема с установкой pandas

Если вы видите ошибку:
```
error: Microsoft Visual C++ 14.0 or greater is required
```

Это означает, что pip пытается скомпилировать pandas из исходников, что требует компилятор C++.

## ✅ Решение 1: Использовать предкомпилированные wheels (Рекомендуется)

### Вариант A: Автоматическая установка

1. Запустите файл `install_requirements.bat`
2. Скрипт автоматически установит все зависимости с предкомпилированными wheels

### Вариант B: Ручная установка

Откройте PowerShell или Command Prompt и выполните:

```bash
# Обновите pip
python.exe -m pip install --upgrade pip

# Установите python-dotenv
python.exe -m pip install python-dotenv

# Установите pandas с предкомпилированными wheels
python.exe -m pip install --only-binary :all: pandas

# Установите остальные зависимости
python.exe -m pip install openpyxl python-telegram-bot
```

### Вариант C: Использовать более новую версию pandas

Обновите `requirements.txt` и используйте более новую версию pandas, которая имеет предкомпилированные wheels:

```bash
python.exe -m pip install -r requirements.txt
```

## ✅ Решение 2: Установить Microsoft C++ Build Tools

Если вы хотите компилировать из исходников:

1. Скачайте и установите [Microsoft C++ Build Tools](https://visualstudio.microsoft.com/visual-cpp-build-tools/)
2. При установке выберите "C++ build tools"
3. После установки перезапустите компьютер
4. Выполните: `pip install -r requirements.txt`

## ✅ Решение 3: Использовать conda (Альтернатива)

Если у вас установлен Anaconda или Miniconda:

```bash
conda install pandas openpyxl
pip install python-telegram-bot python-dotenv
```

## 🔍 Проверка установки

После установки проверьте, что все модули установлены:

```bash
python.exe -c "import telegram; import pandas; import openpyxl; import dotenv; print('Все модули установлены!')"
```

Если команда выполнилась без ошибок - все готово!

## 🚀 Запуск бота

После успешной установки:

1. Создайте файл `.env` (или запустите `setup_local.bat`)
2. Запустите бота: `python bot.py`

## ❓ Проблемы?

### "ModuleNotFoundError: No module named 'dotenv'"

**Решение:**
```bash
python.exe -m pip install python-dotenv
```

### "ModuleNotFoundError: No module named 'pandas'"

**Решение:**
```bash
python.exe -m pip install --only-binary :all: pandas
```

### "ModuleNotFoundError: No module named 'telegram'"

**Решение:**
```bash
python.exe -m pip install python-telegram-bot
```

### Другие проблемы

1. Убедитесь, что используете правильную версию Python (3.11+)
2. Обновите pip: `python.exe -m pip install --upgrade pip`
3. Попробуйте установить зависимости по одной

---

**Рекомендация:** Используйте `install_requirements.bat` - это самый простой способ!

