# Telegram Bot для интервью студентов

Бот для проведения интервью о студенческом дне и сбора данных о проблемах студентов.

## 🚀 Быстрый старт (локально)

1. **Установите зависимости:**
```bash
pip install -r requirements.txt
```

2. **Создайте файл `.env` и добавьте токен:**
```
BOT_TOKEN=your_bot_token_here
```

3. **Запустите бота:**
```bash
python bot.py
```

## 📦 Деплой на сервер (чтобы бот работал 24/7)

Бот может работать на разных платформах. Выберите одну из них:

### 1. Railway (Рекомендуется - простой и бесплатный)

1. Зарегистрируйтесь на [Railway](https://railway.app/)
2. Создайте новый проект
3. Подключите ваш GitHub репозиторий или загрузите код
4. Добавьте переменную окружения:
   - Название: `BOT_TOKEN`
   - Значение: ваш токен бота
5. Railway автоматически определит Python проект и запустит бота
6. Бот будет работать постоянно!

**Примечание:** Railway дает $5 бесплатных кредитов в месяц, чего более чем достаточно для бота.

### 2. Render (Бесплатный, но может засыпать)

1. Зарегистрируйтесь на [Render](https://render.com/)
2. Создайте новый "Web Service"
3. Подключите GitHub репозиторий
4. Настройки:
   - **Build Command:** `pip install -r requirements.txt`
   - **Start Command:** `python bot.py`
   - **Environment:** Python 3
5. Добавьте переменную окружения:
   - Key: `BOT_TOKEN`
   - Value: ваш токен бота
6. Нажмите "Create Web Service"

**Примечание:** На бесплатном плане Render может "засыпать" после 15 минут неактивности. Для бота лучше использовать Railway.

### 3. Fly.io (Хороший вариант)

1. Установите [Fly CLI](https://fly.io/docs/getting-started/installing-flyctl/)
2. Зарегистрируйтесь: `fly auth signup`
3. Создайте приложение: `fly launch`
4. Установите секрет: `fly secrets set BOT_TOKEN=your_token_here`
5. Запустите: `fly deploy`

### 4. PythonAnywhere (Простой вариант для Python)

1. Зарегистрируйтесь на [PythonAnywhere](https://www.pythonanywhere.com/)
2. Создайте новый файл `bot.py` и загрузите код
3. В разделе "Tasks" создайте задачу:
   - Command: `python3.10 /home/yourusername/bot.py`
   - Hour: `*` (каждый час, чтобы бот не засыпал)
4. Установите переменную окружения в "Web" → "Environment variables":
   - `BOT_TOKEN=your_token_here`

### 5. Heroku (Платный, но надежный)

1. Установите [Heroku CLI](https://devcenter.heroku.com/articles/heroku-cli)
2. Зарегистрируйтесь на [Heroku](https://www.heroku.com/)
3. Создайте приложение: `heroku create your-bot-name`
4. Установите секрет: `heroku config:set BOT_TOKEN=your_token_here`
5. Задеплойте: `git push heroku main`

## 📋 Команды бота

- `/start` - Начать новое интервью
- `/export_all` - Скачать таблицу Excel со всеми интервью
- `/stats` - Показать статистику по всем интервью
- `/cancel` - Отменить текущее интервью

## 🔒 Безопасность

- **НЕ коммитьте файл `.env` в Git!** Он уже добавлен в `.gitignore`
- Используйте переменные окружения для хранения токена
- Токен можно получить у [@BotFather](https://t.me/BotFather) в Telegram

## 📊 Структура данных

Все интервью сохраняются в файл `все_интервью.xlsx` автоматически после каждого завершенного интервью.

## 🛠 Технологии

- Python 3.11+
- python-telegram-bot 20.7
- pandas 2.0.3
- openpyxl 3.1.2

## 📝 Лицензия

Этот проект создан для проведения интервью со студентами.

## ❓ Проблемы?

Если бот не запускается:
1. Проверьте, что токен правильно установлен в переменных окружения
2. Убедитесь, что все зависимости установлены: `pip install -r requirements.txt`
3. Проверьте логи на платформе деплоя

